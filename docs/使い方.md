# SampleEqSim 使い方ガイド

**バージョン:** 1.5.0  
**対象:** SampleEqSim.Equipment / SampleEqSim.Host

---

## 1. 前提条件

| 項目 | 要件 |
|------|------|
| OS | Windows 10 / 11 (64bit) |
| 開発環境 | Visual Studio 2022 (17.8 以降) |
| .NET | .NET 10.0 SDK |
| Visual Studio ワークロード | .NET デスクトップ開発 |

---

## 2. セットアップ

### 2.1 ソリューションを開く

1. `SampleEqSim.sln` を Visual Studio で開く
2. NuGet パッケージの復元を行う  
   (`ソリューション` 右クリック → `NuGet パッケージの復元`)
3. ソリューション全体をビルド (`Ctrl+Shift+B`)

### 2.2 スタートアッププロジェクトの設定

複数プロジェクト同時起動の設定:

1. ソリューション右クリック → **スタートアッププロジェクトの設定**
2. **マルチ スタートアップ プロジェクト** を選択
3. `SampleEqSim.Equipment` → **開始**
4. `SampleEqSim.Host` → **開始**
5. **OK** をクリック

> 順番は Equipment を先に設定することを推奨 (サーバー側が先に起動するため)

---

## 3. 画面説明

### 3.1 Equipment Simulator (装置側)

> **2つのアプリの見分け方:** Equipment は **ティール (緑青) 系**の配色 + ⚙ アイコン、
> Host は **ブルー (藍) 系**の配色 + 🖥 アイコンで区別されます。ウィンドウタイトルも
> それぞれ `... Simulator [装置]` / `... Simulator [ホスト]` と表示されます。

```
┌──────────────────────────────────────────────────────────────┐
│  ⚙ EQUIPMENT 装置シミュレータ  (Passive / Port 5000)     時刻 │ ← ヘッダー(ティール)
├──────────────────────────────────────────────────────────────┤
│  COMM: ● COMMUNICATING  CTRL: ● ONLINE_REMOTE  PROC: ● IDLE  │ ← 状態バー
├─────────────────────────────┬────────────────────────────────┤
│ 処理状態操作                 │                                │
│ [IDLE][SETUP][READY]        │                                │
│ [EXECUTE][PAUSE]            │                                │
│                             │        通信ログ                 │
│ プロセス値                   │  [HH:MM:SS.mmm] RCV << S1F13  │
│ 温度: ─────── °C            │  [HH:MM:SS.mmm] SND >> S1F14  │
│ 圧力: ─────── kPa           │  ...                           │
│ ロットID / レシピID / [更新] │                                │
│                             │                                │
│ イベント送信                 │                                │
│ [LotStarted] [LotCompleted] │                                │
│                             │                                │
│ レシピ管理 (S7)              │                                │
│ PPID│Size│更新時刻│プレビュー │                                │
│ ※ ホストから S7F3 で受信     │                                │
│                             │                                │
│ アラーム管理                 │                                │
│ ALID│コード│状態             │                                │
│ [AL1 SET][AL2 SET][全クリア] │                                │
│                             │                                │
│ ポート管理 (E87)             │                                │
│ ID│名前│状態│モード│キャリアID│                                │
│                             │                                │
│ キャリア管理 (E87)           │                                │
│ キャリアID│PortID│状態        │                                │
│ ID: [CARRIER001]  P: [1]    │                                │
│ [ロード]  [アンロード] P:[1] │                                │
└─────────────────────────────┴────────────────────────────────┘
```

> 左パネルはスクロール可能です。項目が多い場合は下にスクロールしてください。

**状態インジケーター (LED色):**

| 色 | 意味 |
|----|------|
| 緑 | 正常稼働中 (Communicating / OnlineRemote / Executing) |
| 青 | ローカル制御中 (OnlineLocal / Ready) |
| 黄 | 遷移中 (WaitCR / AttemptOnline / Setup / Pause) |
| 赤 | エラー / オフライン (HostOffline) |
| グレー | 未接続 / 無効 (Disabled / Init / Idle) |

---

### 3.2 Host Simulator (ホスト側)

```
┌──────────────────────────────────────────────────────────────┐
│  🖥 HOST ホストシミュレータ  (Active / 127.0.0.1:5000) | SEL  │ ← ヘッダー(ブルー)
├──────────────────────────────────────────────────────────────┤
│  Model: -  SoftRev: -  DateTime: -  CtrlState: -  Proc: -   │ ← 装置情報バー
├──────────────────────────────┬───────────────────────────────┤
│ 接続管理                      │                               │
│ [S1F1 AYT][S1F13 通信確立]   │        通信ログ                │
│ [S1F17 ON-LINE][S1F15 OFF]   │  時刻     │ メッセージ          │
│                              │  HH:MM:SS │ SND >> S1F1 ...    │
│ 装置制御                      │  HH:MM:SS │ RCV << S1F2 ...    │
│ [S2F17 日時取得]              │  ...                          │
│ S2F41: [コマンド] [送信]      │                               │
│ [START][STOP][PAUSE]         │                               │
│                              │                               │
│ データ収集設定                 │                               │
│ [S2F33 レポート定義]           ├───────────────────────────────┤
│ [S2F35 リンク][S2F37 有効化]  │   イベント・アラームログ         │
│                              │  [HH:MM:SS] [CARRIER IN] ...  │
│ ステータス変数 (S1F3)          │  [HH:MM:SS] [EVENT] CEID=101  │
│ SVID: [101,102,103]          │  [HH:MM:SS] [ALARM SET] ...   │
│ [S1F3 SV取得]  結果: ...     │                               │
│                              │                               │
│ レシピ管理 (S7)               │                               │
│ PPID: [RECIPE001]            │                               │
│ ボディ: [テキストエリア]        │                               │
│ [S7F1+F3 送信][F17 削除]     │                               │
│ [S7F19 一覧取得]              │                               │
│ 格納済: RECIPE001, ...        │                               │
│                              │                               │
│ アラーム管理                  │                               │
│ [S5F5 一覧取得]               │                               │
│                              │                               │
│ ポート/キャリア管理 (S14/E87)  │                               │
│ [S14F1 ポート属性取得]         │                               │
│ [S14F1 キャリア属性取得]       │                               │
│ 結果: [テキスト表示]           │                               │
│ ※ キャリアIN/OUTはイベントログ │                               │
└──────────────────────────────┴───────────────────────────────┘
```

---

## 4. 基本的な通信テスト手順

### Step 1: アプリ起動

1. `F5` でデバッグ開始 (Equipment・Host が同時起動)
2. Equipment の状態バーが `COMM: DISABLED` であることを確認
3. Host の接続状態が `CONNECTING` → `SELECTED` に変わることを確認
4. Equipment の状態バーが `COMM: WAIT_CR_FROM_HOST` に変わることを確認

### Step 2: 通信確立

Host 側で操作:

1. **[S1F1 Are You There]** をクリック
   - Equipment のモデル名・SoftRev が装置情報バーに表示される
2. **[S1F13 通信確立]** をクリック
   - Equipment の `COMM` が `COMMUNICATING` に変わる
3. **[S1F17 ON-LINE]** をクリック
   - Equipment の `CTRL` が `ONLINE_REMOTE` に変わる

### Step 3: データ収集設定

Host 側で操作:

1. **[S2F33 Define Report]** をクリック
   - RPTID=1 に SVID 101(温度)・102(圧力)・103(ロットID) を登録
2. **[S2F35 Link Event Report]** をクリック
   - CEID 101(ProcessStarted)・102(ProcessCompleted) に RPTID=1 をリンク
3. **[S2F37 Enable All Events]** をクリック
   - 全コレクションイベントを有効化

### Step 4: 処理シミュレーション

Equipment 側で操作:

1. `ロットID` に任意の文字列を入力 (例: `LOT-001`)
2. **[値を更新]** をクリック
3. **[LotStarted]** をクリック → Host のイベントログに `CEID=103` が表示
4. **[EXECUTE]** ボタンをクリック → 処理状態が `EXECUTING` に変化
   - Host のイベントログに `CEID=101` (ProcessStarted) が表示される
5. 温度・圧力スライダーで値を変更し **[値を更新]**
6. Host 側で **[S1F3 SV取得]** をクリック → 現在の SV 値を確認
7. **[IDLE]** をクリック → 処理状態が `IDLE` に変化
   - Host のイベントログに `CEID=102` (ProcessCompleted) が表示

### Step 5: アラームテスト

Equipment 側で操作:

1. **[AL1 SET]** をクリック
   - Host のイベントログに `[ALARM SET] ALID=1 低圧縮空気検出` が表示
2. **[全クリア]** をクリック
   - Host のイベントログに `[ALARM CLR]` が表示

---

## 5. レシピ管理 (S7 プロセスプログラム)

### ホストからレシピを送信する

1. Host 側の「レシピ管理」パネルで **PPID** を入力 (例: `RECIPE001`)
2. **レシピボディ** テキストエリアにレシピ内容を入力  
   例: `STEP1,TEMP=200,TIME=60`
3. **[S7F1+F3 PP送信]** をクリック
   - S7F1 で送信許可を問い合わせ → S7F2(PPGNT=0) が返れば自動的に S7F3 送信
   - Equipment 側の「レシピ管理」DataGrid に新しい行が追加される
   - Host のイベントログに `CEID=201` (ProcessProgramCreated) が表示

### レシピ一覧を取得する

1. **[S7F19 一覧取得]** をクリック
   - 「格納済:」欄に装置内のレシピ PPID が一覧表示される

### レシピを削除する

1. PPID を確認して入力欄に設定
2. **[S7F17 PP削除]** をクリック
   - Equipment 側の DataGrid から行が削除される
   - Host のイベントログに `CEID=202` (ProcessProgramDeleted) が表示

---

## 6. キャリア・ポート管理 (E87)

### キャリアをロードする (Equipment 側操作)

1. Equipment 側の「キャリア管理」パネルで **ID** にキャリアID を入力 (例: `CARRIER001`)
2. **P:** にポート番号を入力 (例: `1`)
3. **[ロード]** をクリック
   - ポート管理 DataGrid のポート状態が `ReadyToUnload` に変わる
   - キャリア管理 DataGrid に新しい行が追加される
   - Host のイベントログに `[CARRIER IN] CarrierID=CARRIER001 Port=1` が表示される

### キャリアをアンロードする (Equipment 側操作)

1. **アンロード** ボタン横の **P:** にアンロードするポート番号を入力
2. **[アンロード]** をクリック
   - ポート状態が `InService` に戻る
   - キャリア管理 DataGrid から行が削除される
   - Host のイベントログに `[CARRIER OUT]` が表示される

### ポート・キャリア属性を取得する (Host 側操作)

1. Host 側の「ポート/キャリア管理」パネルで **[S14F1 ポート属性取得]** をクリック
   - 全ポートの PortID / PortState / AccessMode / CarrierID が結果欄に表示される
2. **[S14F1 キャリア属性取得]** をクリック
   - 現在ロード中のキャリアの CarrierID / PortID / CarrierState が結果欄に表示される

---

## 7. ホストコマンド (S2F41) によるリモート制御

Host 側の「装置制御」パネルから送信できます。

| コマンド | 操作 | 動作 |
|---------|------|------|
| START | テキストボックスに `START` → [送信] | 処理状態を EXECUTING に遷移 |
| STOP | テキストボックスに `STOP` → [送信] | 処理状態を IDLE に遷移 |
| PAUSE | テキストボックスに `PAUSE` → [送信] | 処理状態を PAUSE に遷移 |
| RESUME | テキストボックスに `RESUME` → [送信] | 処理状態を EXECUTING に再開 |
| LOCAL | テキストボックスに `LOCAL` → [送信] | 制御状態を OnlineLocal に遷移 |
| REMOTE | テキストボックスに `REMOTE` → [送信] | 制御状態を OnlineRemote に遷移 |

---

## 8. 設定変更

### 8.0 アプリ画面から接続設定を変更する（推奨・実機接続向け）

両アプリの左パネル先頭の **「接続設定 (HSMS)」** から、JSON を手で編集せずに変更できます。

1. **IPアドレス** / **ポート** / **DeviceId** を入力
2. **Active モード** チェック … ON=自分から相手へ接続(Active) / OFF=待受(Passive)
3. **[保存して再接続 (再起動)]** をクリック → 確認後、`appsettings.json` に保存してアプリが自動再起動し、新しい設定で接続します

ヘッダー右側に現在の設定（例: `Active → 192.168.0.50:5000 (DeviceId=1)`）が表示されます。

> - HSMS の Active/Passive は GEM の装置/ホスト役割とは独立です。どちらのアプリも Active・Passive どちらにも設定できます。
> - 一方を Active、もう一方を Passive にしてください（両方 Active / 両方 Passive では接続できません）。
> - 実機接続では **DeviceId を実機と一致**させてください。
> - 保存先は exe と同じフォルダの `appsettings.json` です（再ビルドすると上書きされる点に注意）。

以下 8.1〜8.3 は、ファイルを直接編集する従来方法です。

### 8.1 接続先 IP・ポートの変更

Equipment を別 PC で動作させる場合:

**Host 側 `appsettings.json`:**
```json
{
  "secs4net": {
    "IsActive": true,
    "IpAddress": "192.168.1.100",
    "Port": 5000
  }
}
```

**Equipment 側 `appsettings.json`:**
```json
{
  "secs4net": {
    "IsActive": false,
    "IpAddress": "0.0.0.0",
    "Port": 5000
  }
}
```

### 8.2 装置モデル名・バージョンの変更

**Equipment 側 `appsettings.json`:**
```json
{
  "Equipment": {
    "ModelName": "MyEquipment",
    "SoftRev": "2.0.0"
  }
}
```

### 8.3 タイムアウト値の変更

T7 (通信確立タイムアウト) を変更する場合:

**Equipment 側 `appsettings.json`:**
```json
{
  "secs4net": {
    "T7Timeout": 30000
  }
}
```

または Host 側から `S2F15` (EC 設定) で ECID=1 (EstablishCommunicationsTimeout) を変更。

---

## 9. ログの見方

### 9.1 通信ログ

| プレフィックス | 意味 |
|--------------|------|
| `SND >> S1F13` | ホスト→装置 送信 |
| `RCV << S1F14` | 装置→ホスト 受信 |
| `[HSMS]` | HSMS レイヤーの状態変化 |
| `[ComState]` | 通信状態遷移 |
| `[CtrlState]` | 制御状態遷移 |
| `[ProcState]` | 処理状態遷移 |
| `[CMD]` | ホストコマンド受信 |
| `[ALARM SET/CLR]` | アラーム発生/クリア |
| `[E87]` | キャリア/ポート操作 |
| `[CARRIER IN/OUT]` | キャリアロード/アンロード通知 |
| `[TERMINAL]` | 端末メッセージ |
| `[ERR]` | エラー |
| `[WARN]` | 未実装メッセージ受信 |

### 9.2 ログ色 (Host 側)

| 色 | 意味 |
|----|------|
| 青 | 送信メッセージ |
| 緑 | 受信メッセージ |
| 赤 | アラーム |
| オレンジ | エラー |
| 白 | システム情報 |

---

## 10. 単体テストの実行

```
Visual Studio のテストエクスプローラー から実行
またはターミナルで:

cd SampleEqSim
dotnet test tests/SampleEqSim.Tests/SampleEqSim.Tests.csproj
```

---

## 11. よくある問題

### Q. アプリがダブルクリックで起動直後に消える / 何も表示されない

起動時の例外は exe と同じフォルダの **`startup-error.log`** に記録されます。
同時にエラーダイアログも表示されます。ログの先頭行 (例外メッセージ) を確認してください。
よくある原因は次の2つです。

- **`appsettings.json` が見つからない** → 出力先にコピーされているか確認 (後述)
- **ポート使用中** → 下記参照

### Q. 「ポート 5000 は既に使用されています」と表示される

Equipment (Passive) は起動時にポート 5000 を Bind して待ち受けます。
別の Equipment シミュレーターが既に起動しているとこのエラーになります。

- タスクマネージャーで `SampleEqSim.Equipment.exe` を終了してから再起動
- 使用プロセスの確認: コマンドプロンプトで `netstat -ano | findstr :5000`

### Q. Host が接続できない

- Equipment が先に起動していることを確認 (Equipment が待受側)
- ファイアウォールで Port 5000 (TCP) が許可されているか確認
- `appsettings.json` の IP・ポートが一致しているか確認

### Q. 通信確立後に S6F11 (イベント) が届かない

以下の順序で設定が必要:
1. `S2F33` でレポートを定義
2. `S2F35` でイベントとレポートをリンク
3. `S2F37` でイベントを有効化

### Q. appsettings.json が見つからないエラー

Visual Studio でプロジェクトの `appsettings.json` のプロパティを確認:
- **出力ディレクトリにコピー:** 「常にコピーする」に設定

### Q. レシピ送信が拒否される (PPGNT≠0)

装置側の処理状態が `Executing` の場合はレシピ送信が拒否されることがあります。  
`IDLE` や `READY` 状態で送信してください。

### Q. キャリアがロードできない

- ポート状態が `InService` または `ReadyToLoad` であることを確認
- すでに同じキャリアIDが登録されていないか確認

### Q. 実機装置と接続したい

**Host Simulator を実機装置に接続する場合:**
- Host の `appsettings.json` の `IpAddress` を実機の IP に変更
- `IsActive: true` のまま使用

**実機ホストに Equipment Simulator を接続する場合:**
- Equipment の `appsettings.json` の `Port` を実機ホストが待受けているポートに変更
- 実機ホストが Active の場合、Equipment は Passive のままで動作

---

## 12. シーケンス監視（想定順序との照合・実機デバッグ）

両アプリ左パネルの **「シーケンス監視」** で、想定するメッセージ順序と実際の通信を
リアルタイム照合できます。「ここがスキップされて次が来た＝異常」を一目で確認できます。

### 表示の見方

各ステップの LED 色：

| 色 | 意味 |
|----|------|
| グレー | 未到達 |
| 黄 | ← 次に期待しているメッセージ |
| 緑 | 受信/送信済（受信時刻を表示） |
| 赤 | **スキップ**（期待されたのに来ないまま次が来た）または逸脱 |

パネル上部のサマリ：
- `監視中… 次に期待: S1F13` … 正常進行中
- `✅ シーケンス完了` … 全ステップ完了
- `⚠ 逸脱検出 (...)` … スキップ／想定外メッセージあり（赤の段階を確認）

### 想定シーケンスの定義（読み込ませる）

exe と同じフォルダの **`sequence.json`** を読み込みます。初回起動時に既定（ONLINE 確立）が
自動生成されるので、これを編集すれば任意の手順を監視できます。

```json
{
  "name": "ONLINE確立シーケンス",
  "steps": [
    { "sf": "S1F13", "desc": "通信確立要求 (Host→Eq)" },
    { "sf": "S1F14", "desc": "通信確立応答 (Eq→Host)" },
    { "sf": "S1F17", "desc": "オンライン要求 (Host→Eq)" },
    { "sf": "S1F18", "desc": "オンライン応答 (Eq→Host)" }
  ]
}
```

- `sf` … 監視するメッセージ（`S<ストリーム>F<ファンクション>`）を順番に列挙
- `desc` … 表示用の説明（任意）
- 編集後はパネルの **[再読込]** で反映、**[リセット]** で監視状態を初期化

> 例: レシピ送信を監視したい場合は `S7F1, S7F2, S7F3, S7F4` を並べる、など。

### 使用例（ONLINE シーケンスの確認）

1. 両アプリを起動（監視パネルは全ステップ グレー、先頭が黄）
2. Host で **[S1F13 通信確立]** → 監視パネルの S1F13・S1F14 が緑、S1F17 が黄に
3. Host で **[S1F17 ON-LINE]** → S1F17・S1F18 が緑、`✅ シーケンス完了`
4. もし S1F13 を飛ばして S1F17 を送ると、S1F13/S1F14 が **赤（スキップ）** になり `⚠ 逸脱検出`

---

## 13. 拡張方法

### 新しいステータス変数を追加

`GemEquipmentModel.cs` の `InitializeGemData()` に追加:

```csharp
StatusVariables[200] = new StatusVariable(200, "MyVariable", "A", () => "値", "unit");
```

### 新しいアラームを追加

```csharp
Alarms[10] = new AlarmDefinition(10, "MY_ALARM", "アラームテキスト", AlarmCategory.Warning);
```

### 新しいコレクションイベントを追加

```csharp
CollectionEvents[200] = new CollectionEventDefinition(200, "MyEvent");
EnabledEvents[200] = true;
```

イベント送信:
```csharp
await _model.SendCollectionEventAsync(200);
```

### 新しいリモートコマンドを追加

`GemEquipmentModel.cs` の `HandleS2F41()` の switch 式に追加:

```csharp
"MYCOMMAND" => ExecuteMyCommand(),
```

### ロードポートを追加する

`GemEquipmentModel.cs` の `InitializeGemData()` に追加:

```csharp
Ports[3] = new LoadPort(3, "LoadPort3");
```
